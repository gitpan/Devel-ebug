use warnings;
use strict;
use lib 'lib';
use Devel::ebug::HTTP;

my $filename = shift;
die "Usage: ebug_http filename\n" unless $filename;

my $server = Devel::ebug::HTTP->new();
$server->port(8080);
$server->program($filename);
$server->run();
